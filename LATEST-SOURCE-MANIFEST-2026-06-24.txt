CiteOX 最新源码清单 - 2026-06-24

用途：
- 这是给 GitHub / Render / Cloudflare 使用的最新源码基准。
- Render 后端服务应使用仓库根目录的 render.yaml，rootDir 为 backend。
- Cloudflare Pages 前端可使用 frontend 目录中的静态文件和 _redirects / _headers。

后端关键修复：
- backend/src/app.ts 同时挂载 /api/auth 和 /api/v1/auth。
- backend/src/routes/auth.ts 中 check-username、validate-industry、send-verify-code 属于公开路由，不需要登录。
- backend/package.json 增加 test:doc01，用于验证注册登录 DOC01 合同。
- backend/tests/doc01AuthPublicRoutes.test.ts 验证公开路由未被 requireAuth 保护。
- backend/tests/doc01AuthHttpContract.test.ts 验证 /api/v1/auth/validate-industry 公开、/api/v1/auth/me 仍需登录。
- backend/tests/doc01FrontendContract.test.ts 验证前端使用 /api/v1/auth/*。

前端关键修复：
- frontend/GEOFlow-Integrated-Final-White.html 使用 /api/v1/auth/*。
- 注册页手机号显示为“手机号（必填）”。
- 注册草稿 key 为 citeox_register_draft。
- frontend/智引GEO-官网宣传页.html 的注册/登录链接使用无斜杠系统页路径。
- frontend/_redirects 处理 /register、/login、/dashboard、/admin。

本地验证命令已运行：
- npm run test:doc01
- npm run prisma:validate
- npm run build
- npm run test:source-hub

验证结果：
- DOC01 auth public route checks passed.
- DOC01 auth HTTP contract checks passed.
- DOC01 frontend contract checks passed.
- Prisma schema valid.
- TypeScript build succeeded.
- Source Hub config policy regression tests passed.

当前限制：
- 本机没有 git、gh、render 命令，也没有可见 Render/GitHub token，因此无法从当前环境直接推送或触发 Render 部署。
- 线上 citeox-api.onrender.com 仍返回 /api/v1/auth/check-username 401，说明 Render 仍在运行旧后端部署。
